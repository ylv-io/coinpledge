import React from 'react';
import { BrowserRouter, Route, Switch, Link, NavLink } from 'react-router-dom';

const NotFoundPage = () => (
  <div className="container has-text-centered">
    <span className="is-size-1">404</span>
  </div>
);

export default NotFoundPage;